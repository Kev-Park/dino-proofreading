from setuptools import setup, find_packages

setup(
    name='dinoproof',
    version='0.1',
    packages=find_packages(),
    install_requires=[],
    author='Kevin Park',
    description='Library for DINO-based connectomic proofreading',
)